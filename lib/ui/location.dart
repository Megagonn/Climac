import 'package:flutter/material.dart';

class AllowLocationModal extends StatelessWidget {
  const AllowLocationModal({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}